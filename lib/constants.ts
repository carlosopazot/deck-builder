/** Minimum query length before triggering a card search */
export const MIN_SEARCH_LENGTH = 2;

/** Maximum copies of a single non-basic-land card allowed per deck */
export const MAX_CARD_QUANTITY = 4;

/** Debounce delay in ms for card search input */
export const CARD_SEARCH_DEBOUNCE_MS = 400;
